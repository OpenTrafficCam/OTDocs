# Public SSH key for GitHub Actions deployment.
# Generate a keypair with:
#   ssh-keygen -t ed25519 -C "github-actions-deploy" -f preview_key
# Store the private key (preview_key) as PREVIEW_SSH_PRIVATE_KEY GitHub Secret.
# Paste the public key (preview_key.pub) here:
AUTHORIZED_KEY=ssh-ed25519 AAAA... github-actions-deploy
